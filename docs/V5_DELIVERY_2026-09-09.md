# Entrega e continuidade da V5 — 9 de setembro de 2026

## Objetivo e autorização vigentes

O responsável pediu concluir a V5 pública em produção e autorizou expressamente GitHub,
integração e publicação. Confirmou o workspace Render **Transparencia Total**.
Não voltar a pedir estas autorizações. Preservar os requisitos de revisão humana dos dados,
fontes oficiais, privacidade e recuperação; autorização não é evidência de funcionamento.

## Checkpoint em curso

- Diretório: `transparencia-total-v4-final`, branch `codex/v5.54-exact-contract-party-candidates`.
- Base confirmada: `a99818d`, PR #135, V5.53 integrada em `main`.
- V5.54 completa a proposta privada por identificador protegido exato entre duas publicações
  ativas; frontend, API, migração, privacidade, concorrência e documentação.
- PostgreSQL local descartável na porta 55454; base `tt_v554_test`, com marcador de teste.
- Runtime Node disponível: 24.19.0 no runtime fornecido pelo Codex. O Python local é 3.12.14;
  a prova oficial Python 3.13.15 tem de vir do CI, sem alterar a política do projeto.
- 33 migrações aplicadas desde zero em `tt_v554_final_test`, incluindo prova SQL canónica.
- 745 testes backend e 159 contratos frontend aprovados; nenhuma integração ignorada.
- Ruff, formatação, mypy (127 módulos), TypeScript e ESLint dos ficheiros alterados aprovados.
- Esquema Prisma validado; build e CI remoto registados abaixo quando terminarem.

## Produção e recuperação

- Supabase de produção: `kxvgungbqalofbqytwbn`, PostgreSQL 17, saudável.
- Consulta de 09-09 confirmou ausência de `staff_profiles` e do esquema V5.54.
- Render: `srv-d9n2h3qjnfac73a8o2bg`, repositório correto, branch `main`, publicação automática.
- Backup real aprovado: execução `34211076816` de 08-09-2026.
- Restauro isolado concluído: execução `34301070176`, sobre esse backup exato.
  Resultado `PASS_WITH_OPERATIONAL_WARNING`: integridade dos 134 objetos de arquivo, hashes,
  13 migrações, 54 tabelas e 519 445 linhas aprovadas. Estado operacional `ATTENTION_REQUIRED`;
  não equivale a prontidão operacional total. RTO 84 segundos; RPO observado 57 988 segundos.
  Atestação `0ecec84bfc698d9236f95d79314a3d5c10a8bdb3fd6b2fdf78c3cfd0ddc06c55`.
- A identidade privada foi validada contra a chave pública e reposta temporariamente no secret
  `BACKUP_AGE_IDENTITY` do environment `recovery`. Não copiar o seu valor para este documento,
  logs, commits ou mensagens. O secret foi removido após o ensaio e a ausência foi verificada.
- Não houve migração de produção nem publicação de dados reais nesta sessão até este checkpoint.

## Próximas ações concretas

1. Terminar os testes da prova SQL final, contratos frontend, lint, tipos e build.
2. Registar a conclusão do restauro, guardar a atestação sanitizada e remover o secret temporário.
3. Fazer commit, push, PR e integrar apenas após CI verde; verificar os deployments resultantes.
4. Fechar inventário e ensaio das migrações antes de aplicar em produção; configurar e provar Auth/MFA.
5. Concluir os requisitos editoriais e de dados das issues #52–#58, incluindo a decisão V5.55,
   sem transformar candidatos privados em relações públicas automáticas.
6. Só criar `v0.5.0` quando os gates reais da checklist estiverem demonstrados.

Os ficheiros `Untitled-*.txt` do diretório pai são privados e não pertencem ao repositório.


## Continuação confirmada — publicação e acesso

- PR #136 integrada; `main` em `159bf067d7380c65a56b6a705793aa3510de409b`.
- CI principal `34349658569` e smoke público `34349755426` aprovados.
- Render publicou esse commit; o arranque foi alterado para `uvicorn app.main:app --host 0.0.0.0 --port $PORT`.
  Deploy `dep-dagpluu1egvs73aufv3g` aprovado, sem reinstalar o rollout V4 em cada arranque.
- `SUPABASE_URL` configurada na API pelo conector, preservando as restantes variáveis.
- Vercel recebeu as duas variáveis públicas Supabase apenas em Production; redeploy iniciado e
  `/auth/entrar` verificado com formulário ativo no domínio público.
- Supabase: Site URL corrigido de localhost para o domínio público, redirect exato do painel
  configurado, registo público desativado e assinatura ES256 comprovada no JWKS público.
- Conta indicada pelo responsável convidada e confirmada. Email e UUID não são registados aqui.
  MFA ainda não concluído: a preparação exige primeiro o perfil editorial na base V5.
  Não pedir ao responsável novos códigos nem capturar o seu QR.
- Backup novo `34350224391` aprovado:
  `database/daily/2026/09/09/transparencia-total-20260909T121913Z-34350224391-1.dump.age`;
  SHA cifrado `f6c857cbfd345e6eb4381dc401832fa5205199f74d9f84bcc47016106c6a32d2`;
  SHA manifesto `987b1e54800124c3ac580bf2d3ad4058f5700dde64f93f75dba0aeba1931366e`.
- A chave de recuperação foi novamente reposta temporariamente para os ensaios desta continuação.
  Remover `BACKUP_AGE_IDENTITY` após terminar e verificar ausência.

## Trabalho operacional em revisão (#137)

Branch `codex/v5-production-migration-rehearsal`. Há workflow de ensaio de migração sobre o
backup real, segundo restauro cifrado de V5 com perfil sintético inativo e operação de produção
limitada ao projeto, ao commit exato e a duas provas de menos de 24 horas.

O primeiro ensaio aplicou as 33 migrações. A comparação identificou a coluna legada vazia retirada
pela V5 e sete checksums históricos que diferem apenas em LF/CRLF e quebras finais. As origens
foram conciliadas contra Git; não se alterou SQL nem o histórico Prisma. O verificador agora
admite essas variantes e continua a recusar mudança de conteúdo SQL. Um problema do índice apt
Chrome do runner foi isolado sem dispensar a verificação das dependências Ubuntu.

O ensaio `34386027439` comprovou a preservação dos dados originais após as 33 migrações.
O segundo restauro identificou uma ligação `psql` sem URI explícito, entretanto corrigida.
O CI `34418690239`, commit `459d85b`, aprovou frontend, backend e um ciclo cifrado completo
de backup/restauro V5 com dados sintéticos e referências Auth inertes.

A repetição real `34418478035` parou antes da transferência por limite diário B2 de downloads
ou transações Class B. Não se aumentou o limite de despesa. Repetir uma vez após a reposição
das 00:00 UTC de 10-09, usando o mesmo objeto e hashes acima, com `rehearse_migrations=true`.
Produção continua com 13 migrações e sem `staff_profiles`; não pedir ainda a configuração MFA.
O Render confirmou a verificação de saúde em `/api/v1/health/ready`.
A branch principal exige PR, CI frontend/backend aprovados, histórico linear e resolução de
conversas, incluindo para administradores; não permite force push nem eliminação.

Depois das duas provas aprovadas: integrar #137, executar `production-schema-migration.yml`
com commit exato de main e run_id do ensaio, verificar a prova e a estrutura remota, associar
apenas a conta autorizada a `staff_profiles` ADMIN, e só então pedir ao responsável concluir MFA.
Fazer backup e restauro posterior à migração. Não declarar a V5 completa nem criar `v0.5.0` sem
fechar os restantes gates operacionais, editoriais e jurídicos da checklist.

## Continuação em 10 de setembro — provas reais aprovadas

- CI `34419038457` aprovado; PR #137 integrada em `e98e4f2`.
- Ensaio real `34419355297` aprovado: 53 tabelas e 519 886 registos originais preservados
  após 33 migrações; segundo restauro V5 aprovado em 78 tabelas, incluindo uma referência
  Auth sintética inativa. RLS e privilégios browser aprovados em ambas as provas.
- Operação de produção `34420032986` recusada pelo validador da ligação antes de qualquer
  alteração. O validador passa a fornecer erros distintos por destino, porta e TLS; quando
  o URI omite TLS, exige `sslmode=require` tanto no verificador como no processo Prisma.
  Modos inseguros explícitos e opções TLS ambíguas continuam recusados.
- Inspeção remota encontrou `reject_v4_rollout_mutation()` sem o `search_path` já definido
  pela migração V4 canónica. Repor `pg_catalog` após a migração e confirmar no advisor.
  O bootstrap V4 que recriava esta função já foi removido do arranque Render.
- Ainda não existe prova de migração de produção nem MFA; continuar pelo workflow protegido,
  usando `34419355297` como ensaio de referência enquanto tiver menos de 24 horas.

PR #138 integrada em `f484b92`. A execução `34420568757` confirmou destino/porta mas parou,
antes das alterações, por falta de confiança na CA Supabase (`SELF_SIGNED_CERT_IN_CHAIN`).
A CA pública oficial foi obtida e validada por TLS 1.3 com verificação de hostname; a próxima
correção fornece-a explicitamente a ambos os clientes sem dispensar verificação de certificados.
O desvio `search_path` V4 foi reconciliado por `reconcile_v4_rollout_guard_search_path` e uma
consulta posterior confirmou `search_path=pg_catalog`, sem função SECURITY DEFINER.

## Continuação em 11 de setembro — esquema V5 em produção

- PRs #139–#141 integradas. A verificação TLS usa a CA pública oficial e confirma o hostname;
  não desativa validação. As esperas de ligação e consulta têm limites e diagnóstico sem segredos.
- A captura de `raw_source_objects` excedia o limite ao converter os bytes grandes para JSON.
  A impressão agora calcula SHA-256 dos bytes reais antes da representação JSON. Não confia no
  campo de hash declarado. CI `34466173379` aprovou a regressão com 16 MiB e alteração dos bytes
  mantendo o hash declarado, além do ciclo cifrado completo.
- Commit operacional: `642d8fc8a698c96af45d9c3abdaea9f04cf2866a`.
- Cópia anterior à migração: `34461932964`, objeto
  `database/daily/2026/09/10/transparencia-total-20260910T094123Z-34461932964-1.dump.age`;
  SHA cifrado `dacd75aa9e436a233168a85ee56f5b2c802da80d12acdfa09db07ccbe2609f19`;
  SHA manifesto `25df6d14ef47570df993648f2f61732aa9f17be6c672aaabec5ecd5b166d40ff`.
- [Ensaio real 34547252155](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34547252155)
  aprovado: 53 tabelas/520 379 registos originais preservados após 33 migrações. Segundo ciclo
  V5 aprovado: 78 tabelas/520 380 registos, incluindo um perfil sintético inativo. Em ambos,
  RLS e privilégios browser seguros; autenticação real não provada por este ensaio.
- [Migração de produção 34547739521](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34547739521)
  aprovada às 00:46 UTC: 33 checksums, 53 tabelas/520 379 registos originais preservados,
  RLS e privilégios browser aprovados. Consulta independente confirmou 33 migrações completas
  e zero incompletas. A coluna retirada `organisations.public_nipc` estava vazia.
- Perfil ADMIN ativo criado apenas para a conta confirmada e autorizada; zero duplicados.
  O responsável foi informado de que já pode concluir o MFA diretamente. Email, UUID, QR e
  códigos não pertencem à documentação nem às provas públicas.
- Advisor de segurança após migração: 79 avisos informativos de RLS sem políticas (negação por
  omissão esperada nas tabelas privadas), um aviso de proteção de passwords comprometidas não
  ativada e nenhum aviso de `search_path` mutável. O login atual é por convite/ligação de email;
  este resultado não substitui o ensaio de MFA ou uma auditoria independente.
- Cópia pós-migração `34547986986` e smoke público `34547988929` iniciados; confirmar resultados
  antes de marcar os respetivos requisitos. Segue-se restauro isolado da nova cópia.
- `BACKUP_AGE_IDENTITY` permanece temporariamente no ambiente `recovery` durante este trabalho;
  remover e confirmar ausência após o último ensaio. O backup público não recupera contas Auth
  nem fatores MFA; as referências inativas de restauro não permitem autenticação.

Os checkpoints anteriores permanecem como histórico. O sucesso desta operação não publica
processos editoriais, não prova staging remoto e não fecha a V5 nem autoriza a tag `v0.5.0`.

### MFA de produção confirmado e correção da imagem

O responsável concluiu a configuração diretamente e confirmou o acesso à fila editorial como
administrador, com “MFA ativo”. Consulta independente confirmou um fator TOTP verificado e zero
pendentes. A fila sem processos não é uma falha nem prova de ingestão/publicação concluída.

O QR não aparecia porque o componente acrescentava `data:image/svg+xml` a uma resposta que o
SDK instalado já devolve como data URL. A correção preserva a resposta do SDK, explica a entrada
manual e mantém a chave alternativa recolhida num detalhe expansível. O teste de regressão usa
o SDK instalado e uma resposta exclusivamente sintética, sem contas ou fatores reais.

### Publicação da correção e nova cópia

PR #142 integrada em `ed6c7f9ba5aa7d87b542790b0d49ade16c6bf949`. CI do candidato
`34548937630` e CI de main `34597430411` aprovados. Deployment Vercel
`dpl_2dUMAs2b4QMj2BAseeAjvsJn2w1D` em Production/READY, com o domínio oficial associado;
Render `dep-dahuujuk1f9s73fbhl3g` live no mesmo commit. Smoke pós-deployment `34597473551`
aprovado. A imagem foi também testada num Chromium isolado com o componente real e resposta
sintética do SDK instalado: imagem carregada, entrada manual disponível, chave recolhida por
omissão e zero erros do navegador. O artefacto público confirma a utilização direta da data URL.

Cópia pós-migração `34547986986` aprovada em 11-09-2026, com 232 201 161 bytes cifrados:

- objeto `database/daily/2026/09/11/transparencia-total-20260911T004843Z-34547986986-1.dump.age`;
- SHA cifrado `5b08bae718a66441ecb21fe5bddd7a7347657389fdbe907b204733206794aa58`;
- SHA manifesto `a6bef15c3ea4b74015fc4d6e4d0ee65124ea74a35daa8a08cc5a5fa94a89ff42`.

O restauro isolado desta cópia `34597421996` terminou com integridade aprovada e aviso operacional,
conforme a prova abaixo. Em 11-09-2026, o responsável decidiu não publicar ainda nome nem email.
Preservar esta preferência: não configurar esses campos públicos nem reutilizar os dados da
conta administrativa. Os requisitos de identificação/contacto permanecem pendentes; esta decisão
não constitui resolução automática dos restantes pontos da avaliação jurídica ou da história Git.

### Recuperação pós-migração concluída

[Atestação versionada](RESTORE_ATTESTATION_2026-09-11.json), SHA canónico
`d3ad39135905dc7c92b9107e18162827e657fac045e2c0a1c5db741fb6226361`:

- resultado `PASS_WITH_OPERATIONAL_WARNING`: cifragem, contagens, migrações e 142 objetos do
  arquivo aprovados; estado operacional da cópia `ATTENTION_REQUIRED`, preservado como aviso;
- 79 tabelas e 520 413 linhas incluindo as 33 linhas da história Prisma;
- comparação de conteúdo: 78 tabelas e 520 380 linhas, excluindo essa história, todas preservadas;
- 33 checksums, RLS e privilégios browser aprovados; segundo ciclo cifrado também `PASS`,
  com uma linha sintética adicional e referências Auth inertes;
- produção nunca usada como destino; RTO do restauro inicial 95 segundos e idade da cópia no
  início do ensaio (RPO observado) 39 996 segundos. Estes tempos não são uma garantia futura;
- `BACKUP_AGE_IDENTITY` eliminado do ambiente `recovery`, ausência confirmada por consulta
  dos nomes de Secrets; identidade de recuperação antiga preservada fora do repositório;
- smoke manual frontend/API `34597712732` aprovado após os deployments do commit `ed6c7f9`.

A preferência de não publicar nome/email revelou um valor pessoal predefinido antigo em
`lib/site.ts`. A correção remove esse fallback e apresenta a identificação como ainda não
publicada. Um teste de comportamento verifica valores ausentes, vazios e uma configuração
exclusivamente sintética. Não reescreve a história Git nem divulga os dados administrativos.

### Identificação pública removida e diagnóstico parlamentar

PR #143 integrada em `e6f20eeb463a08a4d2a03ec15cdacdf3f0b5ce5b`; CI main
`34598778316` e smoke pós-deployment `34598828626` aprovados. Uma consulta HTTP das páginas
inicial, contacto, privacidade e termos confirmou ausência do antigo nome predefinido e de
ligações `mailto:`. A preferência do responsável mantém-se em vigor.

A última execução real de `PARLIAMENT_ACTIVITY` em 11-09 às 08:22 UTC falhou antes de ler/gravar
registos: o JSON oficial incluía uma ligação HTTP antiga ao visualizador documental. A variante
HTTPS da mesma origem, caminho e query foi consultada e devolveu HTTP 200, tipo PDF e assinatura
PDF válida. A correção fica limitada a esse visualizador; preserva bytes/hash da fonte e mantém
a guarda geral. Testes cobrem conservação da prova e recusa de outros HTTP, origens parecidas,
credenciais e portas não autorizadas. Entradas HTTPS já aceites conservam o mesmo resultado.

A base de produção mediu 532 106 387 bytes (`507 MB` na apresentação PostgreSQL), e a organização
foi novamente confirmada no plano Free. A [quota oficial](https://supabase.com/docs/guides/platform/database-size)
é 500 MB e pode provocar modo só de leitura. Não iniciar uma nova carga volumosa antes de resolver
a capacidade. Não eliminar arquivos nem desligar restrições do fornecedor para contornar a quota.
O responsável foi consultado sobre manter custos atuais com arquivo externo ou preparar uma
proposta paga; nenhuma alteração de faturação foi autorizada ou executada nesta etapa.

## 12 de setembro de 2026 — correção publicada e reverificação documental

Datas locais Europe/Lisbon; as operações abaixo ocorreram a partir de 11-09 às 23:09 UTC.

- PR #144 integrada em `5b78f2e50de6a049bcf16670e05e003c407a1272` após CI do candidato
  `34599226428` aprovado. CI de main `34656946786` e smoke pós-deployment `34656988673`
  também aprovados.
- Vercel `dpl_4P5iDd6PLQpZkHLBLTJgkCNwHuHh` em `READY`/production e Render
  `dep-dai8k6lckfvc73bs9okg` em `live`, ambos no mesmo commit.
- Smoke manual sobre o domínio oficial e API aprovado às 23:11:04 UTC, 13 páginas. O
  Investigador devolveu o 503 neutro previsto pelo contrato de indisponibilidade; não se afirma
  que o grafo público já esteja operacional.
- Nova consulta read-only: 33 migrações completas, um perfil ADMIN ativo, base de
  532 106 387 bytes e `default_transaction_read_only=off`. Esta configuração não garante que
  o fornecedor não venha a impor uma restrição pela quota.
- Antes de integrar, o workflow parlamentar ID `329979490` foi desativado; uma nova consulta
  confirmou `disabled_manually`. Não havia execução em curso. A última, `34578380348`,
  terminou com falha antes desta correção. Nenhuma nova ingestão foi iniciada.
- Backups e monitorização continuam ativos. O último backup listado nesta verificação,
  `34585182624`, terminou com sucesso; o último restauro comprovado continua a ser
  `34597421996`, da cópia explicitamente identificada na atestação. Não se presume que esse
  restauro tenha ensaiado a cópia mais recente.
- Os 93 documentos atuais foram novamente lidos pelo verificador: zero destinos locais em
  falta e zero ocorrências dos três padrões de segredos pesquisados. Dos 61 URLs externos,
  60 responderam HTTP 200 e a página parlamentar de reuniões plenárias excedeu o tempo de
  espera em duas tentativas. Limites e resultado preservados na revisão documental.

### Capacidade e condições de retoma

`raw_source_objects` ocupa 295 739 392 bytes, `vote_records` 130 834 432 bytes,
`parliamentary_initiatives` 39 067 648 bytes e `vote_events` 30 932 992 bytes, medidos por
`pg_total_relation_size` (inclui índices e armazenamento auxiliar). Estes números fundamentam
o diagnóstico; não autorizam eliminar linhas, prova de origem ou história.

Não foi recebida autorização para aumentar custos. O arquivo externo é uma alternativa por
implementar e ensaiar: conservar os bytes e hashes, verificar leitura a partir do novo suporte,
provar backup/restauro e só depois avaliar qualquer libertação do armazenamento anterior.
Não existe ainda uma estimativa verificada de custo, espaço recuperável ou duração dessa mudança.

Antes de reativar o workflow, provar capacidade para a carga e crescimento, verificar backup
recente e executar uma recolha controlada sem publicação. Confirmar contagens, hashes e monitor
de frescura. A suspensão mantém os dados existentes e o atraso visível; não fecha a cobertura.
Não foram alterados planos, removidos dados ou configurados nome/email públicos.

### Próximas condições da V5 integral

Capacidade; staging separado e ciclo editorial remoto completo; recuperação de Auth/MFA;
consolidação humana e cobertura real; circuitos factuais ainda pendentes, incluindo V5.55;
avaliações de IA e privacidade; decisões jurídicas e identificação/contacto diferidos pelo
responsável; auditoria final de credenciais/história e release comprovada. A checklist mantém
estas condições abertas. Não foi criada a tag `v0.5.0` nem declarado o lançamento integral.

## 12 de setembro de 2026 — verificação da base enquanto a VPS está pendente

O responsável informou que avisará quando tiver a VPS. Não foi contratado servidor nem iniciada
migração para outro fornecedor. A recolha parlamentar continua `disabled_manually` por capacidade.

Consultas de leitura na base existente comprovaram:

- 532 106 387 bytes, 33 migrações completas, zero migrações incompletas, um ADMIN ativo;
- 79 tabelas `public`, todas com RLS e zero privilégios diretos de tabela para `anon` ou
  `authenticated`; os acessos editoriais continuam a passar pela API autorizada;
- zero bloqueios à espera no instante observado; `default_transaction_read_only=off` não
  constitui garantia contra restrições posteriores do fornecedor;
- **145 objetos de arquivo**, 1 697 791 199 bytes lógicos, zero divergências de SHA-256,
  tamanho e chave. O cálculo percorreu os bytes reais no servidor, sem os exportar;
- zero atestações PostgreSQL inconsistentes com o objeto referido;
- zero processos editoriais e zero eventos de publicação V5. A migração e o acesso ao painel
  não significam que já exista conteúdo editorial V5 publicado;
- uma constraint `mandates_period_order_check` permanece `NOT VALID`, conforme a migração
  original; existem zero mandatos e zero períodos invertidos. Não se alterou o esquema nesta
  verificação. A validação formal continua a requerer uma operação própria controlada.

O advisor de segurança mantém 79 avisos informativos de RLS sem política (negação por omissão
esperada) e um aviso sobre proteção de passwords comprometidas desativada. O login atual usa
ligação por email e MFA; isto não elimina nem oculta o aviso. O advisor de desempenho lista
44 FKs sem índice e 114 índices sem uso observado. Não foram eliminados índices nem criados
novos: estes sinais precisam de plano de consultas e carga representativa, sobretudo antes da
migração para VPS. Não se tratam contagens de sugestões como prova de lentidão ou segurança total.

### Correções operacionais preparadas a partir desta verificação

O aviso anterior considerava apenas o arquivo e podia devolver `OK` com a base acima da quota.
A correção mede a base completa, com limite de aviso configurável e independente. O workflow
continua a verificar integridade mesmo perante aviso, preserva ambos os relatórios e mantém
o resultado global em atenção. O verificador de integridade deixou de carregar todos os bytes
na memória do processo; calcula os hashes reais no servidor numa transação exclusivamente de
leitura e devolve apenas metadados. Erros de ligação são sanitizados na saída dos comandos.

Testes dirigidos locais: 17 aprovados, quatro de integração reservados ao PostgreSQL descartável
do CI; sete contratos Node aprovados e lint/formatação aprovados. A integração remota, os testes
com PostgreSQL e a execução real do workflow devem ser confirmados no resultado da PR e no
checkpoint posterior. Estes testes não fecham os restantes requisitos editoriais da V5.

## Continuação em 12 de setembro — Investigador e âmbito das comparações

A PR #146 foi integrada em `653b1f6159f510a1f29cc55c322747f4326919b9`, com CI aprovado
e ambas as aplicações publicadas. A [verificação real do arquivo](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34677139540)
confirmou 145 objetos íntegros, zero divergências e 532 106 387 bytes na base. O resultado
global é de falha por capacidade, como previsto; a integridade passou. A recolha parlamentar
permanece suspensa até existir capacidade adequada.

A investigação posterior identificou a causa concreta do HTTP 503 no Investigador: a consulta
referia `statement_vote_comparisons.current_publication_snapshot_id`, coluna inexistente no
esquema migrado. O 503 registado anteriormente era uma falha de consulta; não comprovava apenas
ausência de conteúdo revisto. A correção exige o evento específico mais recente `PUBLISH`
para `STATEMENT_VOTE_COMPARISON`, preservando os restantes requisitos de revisão, arquivo,
fonte oficial e identidade nominal exata. Uma retirada posterior oculta o par.

Este alvo é uma reserva de leitura com negação por omissão: **não foi implementado um novo
comando editorial de publicação de comparações**, nem foram criados eventos ou dados reais.
As flags legadas isoladas não tornam uma comparação pública. O circuito editorial específico
e as provas de comparabilidade continuam pendentes.

A projeção deixou de juntar o último índice agregado da pessoa sem período ou metodologia
comuns. O contrato identifica `INDIVIDUAL_PAIR`; percentagem e contagens agregadas são `null`.
A interface apresenta todos os pares recebidos, com títulos acessíveis distintos e método
independente, sem calcular exclusões por insuficiência a partir de totais não comprovados.

Validação local: 29 testes Python aprovados; o teste novo contra o esquema migrado exige o
PostgreSQL descartável do CI. Os 165 testes frontend passaram. O navegador real confirmou
dois pares sintéticos, abertura/fecho independente do método, ausência de métricas legadas e
ausência de transbordo a 390 e 1280 px. Nenhuma conta ou publicação real foi usada nesse ensaio.
CI, publicação e resposta pública após a correção devem ser confirmados na PR correspondente.

A revisão integral automática manteve 93 documentos sem destinos locais em falta ou ocorrências
dos três padrões de segredos pesquisados; os 62 URLs externos responderam HTTP 200 nesta passagem.
Não é uma aprovação jurídica nem uma auditoria universal de segredos. A rota pública legada
`/investigador` continua a redirecionar para `/metodologia#neutralidade`; a correção da API e o
ensaio sintético do componente não significam abertura pública do circuito editorial incompleto.

O fecho integral da V5 continua dependente dos itens abertos da
[checklist de lançamento](V5_RELEASE_CHECKLIST.md): capacidade, staging remoto, revisão e
cobertura reais, V5.55, avaliação de IA, requisitos jurídicos e recuperação/privacidade.
O responsável mantém a decisão de não apresentar publicamente nome ou email nesta fase.
