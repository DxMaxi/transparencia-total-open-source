# V5 — ambiente remoto separado e sessões editoriais

## Destino autorizado e custo

Em 12-09-2026 o responsável escolheu a organização `DxMaxi` e confirmou expressamente
a criação de `transparencia-total-staging`, em Frankfurt, pelo custo indicado pelo Supabase
de **0 EUR/mês**, sem ativar planos pagos. O projeto criado é `szckoijgdsznkbbssfbj`.
O projeto de produção permanece `kxvgungbqalofbqytwbn` e não foi usado como destino de ensaio.

O inventário inicial confirmou zero tabelas públicas e zero contas Auth. Foram aplicados
os 33 SQL já versionados no commit `4784d9b410410d53e3419fd386f703f6e39ca69d`, pela ferramenta
de migração Supabase. Cada SQL veio do blob Git, sem mudanças, e a respetiva conclusão foi
registada em `_prisma_migrations` na mesma transação, com o SHA-256 exato do blob. O registo
Prisma e a recusa inicial de privilégios foram preparados previamente apenas neste projeto
vazio. O histórico nativo Supabase conserva também estas operações. Não foi copiada a base real.

Verificação posterior independente:

- 33 migrações concluídas e 33 checksums iguais aos blobs Git;
- 79 tabelas públicas, todas com RLS;
- zero privilégios de tabela para `anon` e `authenticated`;
- zero contas Auth, perfis editoriais, processos e eventos de publicação;
- 16 395 411 bytes na base no instante observado.

O painel confirmou registo de novas contas desativado, login anónimo desativado,
associação manual desativada e confirmação de email ativa. Nenhuma conta real foi convidada.

## Sessões revogadas

A assinatura e validade temporal de um JWT não demonstram que a sessão ainda esteja ativa.
A [documentação oficial Supabase](https://supabase.com/docs/guides/auth/sessions#how-to-ensure-an-access-token-jwt-cannot-be-used-after-a-user-signs-out)
explica a correlação entre `session_id` e `auth.sessions` após terminar sessão.

A API editorial passa a exigir o UUID de sessão assinado e a consultar o estado atual antes
de aceitar um pedido privado: sessão do mesmo utilizador, conta não eliminada nem banida,
prazo da sessão ainda válido e perfil editorial ativo. Em `aal2`, exige também sessão `aal2`
e o respetivo fator verificado ainda associado à mesma conta. O percurso de elegibilidade
`aal1` conserva apenas a possibilidade de preparar MFA; as rotas privadas continuam a exigir
`aal2`. Falhas de consulta recusam o acesso com uma mensagem sanitizada.

Os testes usam PostgreSQL descartável e IDs sintéticos. Cobrem saída, eliminação lógica,
banimento, prazo expirado, fator removido ou não verificado, sessão de outro utilizador,
redução de nível e perfil desativado. Uma nova sessão permite recuperar acesso sem revalidar
o identificador antigo. O bootstrap de Auth do CI é mínimo e não substitui o serviço GoTrue.

## Condições que permanecem abertas

Reverificação de 13-09-2026: mantêm-se 33 migrações concluídas, 79 tabelas com RLS,
zero privilégios browser, zero triggers de aplicação desativados, zero contas e processos.
O endpoint público JWKS publica uma chave de assinatura assimétrica `ES256`/`EC`.
O consultor de segurança Supabase devolveu apenas 79 avisos informativos de
[RLS sem políticas](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy),
coerentes com a ausência deliberada de acesso direto às tabelas pelo navegador; zero avisos
de nível WARNING ou ERROR nessa consulta. Não foram abertas políticas para eliminar esses avisos.

Foi criado o environment `staging` do GitHub, limitado a branches protegidas. Foram instaladas
e relidas apenas as variáveis não secretas `STAGING_SUPABASE_URL`, `STAGING_SUPABASE_PROJECT_REF`
e `STAGING_FORBIDDEN_PROJECT_REFS`, esta última apontando para produção. O responsável configurou
`STAGING_DATABASE_URL` diretamente no environment, incluindo `sslmode=require`.
O valor secreto não foi recuperado nem incluído em relatórios. `STAGING_CORS_ORIGIN` aguarda
a origem real do frontend separado; não foi preenchida com um domínio fictício.

Os dois inspetores read-only podem executar antes de existir essa origem: não arrancam um
servidor HTTP e aceitam ambos os valores CORS ausentes. Se algum valor estiver presente,
validam a configuração completa. Operações de escrita continuam a exigir a origem exata.
Todos os percursos mantêm a validação da base, projeto, destino proibido e confirmação própria.
O [inventário remoto 34781902750](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34781902750)
passou através do session pooler do projeto de staging: transação read-only, PostgreSQL 17,
33 migrações aplicadas, 79 tabelas e 61 funções públicas. A primeira tentativa
[34781680545](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34781680545)
foi recusada antes da ligação por faltar o modo SSL; a configuração foi corrigida pelo responsável.

A [inspeção editorial 34781980396](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34781980396)
passou 15 dos 16 controlos automáticos. O único controlo falhado foi `active_admin`:
existem zero ADMIN e zero REVIEWER ativos. RLS, ausência de privilégios browser, defaults,
`search_path`, nove triggers editoriais e FK para Auth passaram. Este resultado parcial
não comprova JWT real, redirects ou MFA e não autoriza publicação.

A correção de sessões foi integrada pela [PR #148](https://github.com/DxMaxi/transparencia-total-open-source/pull/148).
O CI de `main` [34736591780](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34736591780)
passou com 788 testes backend, PostgreSQL 17, restauro sintético e verificações frontend/browser.
O commit `f319ce98d884c218c98bf629a6ac283271927091` foi confirmado no Render `live` e Vercel
`READY`; o [smoke público manual](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34736799434)
passou após ambos. Esta prova de produção não substitui os percursos remotos abaixo.

A [PR #149](https://github.com/DxMaxi/transparencia-total-open-source/pull/149) acrescentou o
avaliador offline de IA e permitiu os inspetores read-only sem CORS, preservando a exigência
de origem exata nas operações de escrita. O candidato passou 805 testes backend e 166 frontend,
PostgreSQL 17 e restauro sintético no [CI 34737298115](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34737298115).
O commit integrado `0d519828154d103106335dc9fff15c6a5160f8d5` passou o
[CI de main](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34781676348),
foi confirmado no Render `live` e Vercel `READY`, e passou o
[smoke público 34781786900](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34781786900).

## Serviço HTTP separado — 13 de setembro

Foi criado `transparencia-total-api-staging`, no Render, workspace `Transparencia Total`,
região Frankfurt e plano `free`, com deploy automático desligado. O endereço atribuído é
`https://transparencia-total-api-staging.onrender.com` e o serviço é
`srv-dajgta95efls738svr80`. Usa o mesmo código de `main`, `ENVIRONMENT=staging`, URL Supabase
exclusiva de staging, IA desligada e lista CORS vazia enquanto o frontend não tem origem confirmada.
Não recebe credenciais nem dados de produção. A ligação GitHub não pode ser recuperada dos
Secrets para instalação no Render: `DATABASE_URL` tem de ser configurada separadamente.
Criar o serviço ou obter liveness não equivale a readiness da base ou ensaio editorial aprovado.

Em 14-09-2026 o responsável instalou a ligação privada diretamente no Render. Após o deploy
`dep-dajuq05g1s2s73c8u1v0`, as três verificações HTTP passaram: `/api/v1/health/live` 200,
`/api/v1/health/ready` 200 com `database_ready=true` e `/api/v1/health` 200 com
`environment=staging`, base configurada e `ai_provider=disabled`.

Foi também criado o projeto Vercel `transparencia-total-staging`
(`prj_JHEDMULktB1CzSmntEXnR8DKqeNL`) na equipa existente, plano Hobby. Foram instalados apenas
os endereços da API/site de staging, URL Supabase de staging e a sua chave publicável.
Não foi instalada uma ligação PostgreSQL na Vercel. `DEPLOYMENT_ENVIRONMENT=staging`
identifica o projeto mesmo quando a Vercel chama `production` ao seu deployment principal.
O código exclui staging e previews da indexação: cabeçalho `X-Robots-Tag`, robots com
`Disallow: /` e sitemap vazio sem consultar os dados. São instruções de indexação,
não substituem a autenticação e MFA das rotas privadas. A publicação do frontend e
validação do domínio ainda precisam de prova remota.

Continuação de 15-09: os primeiros builds do frontend foram recusados pelo carregamento de
`prisma.config.ts`, que exige `DATABASE_URL` mesmo para gerar o cliente. `db:generate` passa
a executar exclusivamente `prisma generate` num subprocesso com URL local sem credenciais,
substituindo qualquer URL herdada apenas nesse subprocesso. Não liga a uma base, não aplica
migrações e não altera a configuração dos comandos `db:deploy`/`db:migrate`. O CI executa
essa geração com `DATABASE_URL` removida do ambiente. O frontend continua sem credenciais
PostgreSQL reais; os pedidos de dados usam a API de staging.

### Publicação e verificações de 15-09-2026

A [PR #153](https://github.com/DxMaxi/transparencia-total-open-source/pull/153) passou o
[CI 34934194433](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/34934194433)
e foi integrada no commit `23c51fe12a087d4ad60274c25882d40e721c1fc2`. O frontend de staging
foi publicado `READY` como `dpl_CqWDBEAfZM5Z51fvsAE3eKBkc2Ao` e a API separada ficou `live`
como `dep-dakdprvqj5pc73aqfibg` no mesmo commit. Endereços efetivos:

- Site: <https://transparencia-total-staging.vercel.app>;
- API: <https://transparencia-total-api-staging.onrender.com>.

Foram comprovados HTTP 200 em robots, sitemap e acesso privado; `X-Robots-Tag` contém
`noindex, nofollow, noarchive`, robots recusa crawling integral e o sitemap não contém URLs.
O pedido a `/admin/revisao` sem sessão termina em `/auth/entrar?erro=sessao`; o formulário
foi aberto num navegador real e está configurado. A API recusa processos editoriais sem
sessão com HTTP 401. O preflight CORS aceita apenas a origem de staging configurada;
a origem de produção foi recusada com HTTP 400 e sem `Access-Control-Allow-Origin`.

`STAGING_CORS_ORIGIN` já está instalado no environment GitHub. O Site URL de Auth é a origem
de staging e os dois redirects autorizados são `/auth/confirmar` e o mesmo caminho com
`next=%2Fauth%2Fmfa%3Fnext%3D%2Fadmin%2Frevisao`, sem wildcard. As definições públicas de Auth
confirmaram registo desativado e login anónimo desativado. Uma consulta REST a
`editorial_cases` usando exclusivamente a chave publicável foi recusada com HTTP 401,
código PostgreSQL `42501`. Esta é uma prova de recusa anónima, não um ensaio com JWT `aal1`.

O inventário de 15-09 confirmou zero contas Auth, perfis staff, processos e eventos de
publicação, e 16 428 179 bytes na base. A conta ADMIN de ensaio, MFA e os percursos editoriais
autenticados continuam pendentes. Um ambiente publicado e isolado não fecha esses requisitos.

Antes da criação, o painel de faturação mostrou plano Hobby, sem cartão, custo acumulado e
projetado de USD 0, 57,67/750 horas gratuitas, 41 MB/5 GB e 5/500 minutos de builds usados.
É uma observação desse instante. As [horas gratuitas são partilhadas pelo workspace](https://render.com/docs/free);
staging deve servir apenas os ensaios, sem tráfego artificial para o manter acordado.

A criação, migração e isolamento do projeto **não fecham o ensaio editorial remoto completo**.
Continuam necessários: ensaio das sessões emitidas pelo fornecedor, conta
de ensaio com MFA, publicação/retirada e recuperação operacional de Auth/MFA. Não foram
instaladas credenciais de produção neste projeto nem tratados dados de cidadãos.

A V5.55 e os restantes circuitos factuais, cobertura e revisão humanas, avaliações de IA e
requisitos jurídicos permanecem na [checklist](V5_RELEASE_CHECKLIST.md). A política de não
publicar nome/email mantém-se. A VPS não é necessária para as verificações de esquema aqui
concluídas, mas a capacidade de produção continua pendente.

### Conta de ensaio e catálogo privado — 21-09-2026

A PR #154 foi integrada em `6dbefb8b798dd82944b3f61a6455f99201eb07b5`, com
[CI aprovado](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/35592849368).
O frontend de staging ficou `READY` em `dpl_E5FKXPJcVhjeSiKuHtkcdbrSnMtR`;
robots e login responderam HTTP 200 com exclusão de indexação. A API respondeu HTTP 200
em readiness, com `database_ready=true`. O site público também respondeu HTTP 200.

O responsável autorizou expressamente o convite e a atribuição de ADMIN à sua conta de
ensaio. A criação foi confirmada com alias `staging-owner`, função `ADMIN` e estado ativo.
O email e os identificadores privados não integram este registo. O
[inventário 35593034634](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/35593034634)
passou e a [inspeção 35593112200](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/35593112200)
passou os 16 controlos, incluindo o ADMIN que faltava. Esta inspeção prova estrutura e
permissões PostgreSQL; MFA, sessões reais e circuito editorial continuam a exigir ensaio.

O convite inicial do fornecedor confirma a conta e pode terminar na página principal.
O acesso ao painel deve ser iniciado em `/auth/entrar`, seguindo a ligação segura mais
recente no mesmo navegador e concluindo `/auth/mfa`. Não copiar URLs de autenticação,
chaves TOTP ou códigos para relatórios. Foi invalidada uma sessão inicial de staging;
a conta e o perfil foram preservados, com zero sessões remanescentes nessa verificação.
Isto não equivale à prova completa de recuperação Auth/MFA nem de revogação pela API.

O [workflow 35593168205](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/35593168205)
arquivou o Programa do XXV Governo e criou o catálogo exclusivamente privado. A consulta
independente confirmou 1 fotografia, 40 blocos, 1 590 candidatos, zero promessas públicas
e zero eventos de publicação. O SHA-256 da fonte continua
`b309badfd59b990774d1552cae8f19968c3d15f202c65ac0d9c2b9c1e3ae1580` e o catálogo tem hash
`1b30d61e22e23f712d0b2e0f33e083f8028b9b9038ededf247dabf3da6037d36`.
O estado é `STAGED_PRIVATE_PENDING_REVIEW`: critérios e revisão humana permanecem por
concluir. Após esta operação, a base de staging ocupava 24 013 971 bytes; a medição não
altera a situação de capacidade da produção.

O [workflow 35593387248](https://github.com/DxMaxi/transparencia-total-open-source/actions/runs/35593387248)
concluiu também a recolha privada do catálogo anual BASE: uma fotografia, 15 recursos
entre 2012 e 2026, anos terminados até 2025 e ano corrente 2026 provisório. O JSON oficial
arquivado tem 45 985 bytes e SHA-256
`61bc92958d75a0856c99f26638b560d0987b5173728813bc7596a80c16d5c93e`.
A consulta independente confirmou zero lotes de contratos e zero eventos de publicação.
Os ZIP anuais não foram descarregados por esta operação. O inventário temporal não é
prova de cobertura dos contratos, correspondências organizacionais ou grafo público.
